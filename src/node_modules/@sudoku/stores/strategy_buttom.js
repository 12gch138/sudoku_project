import { writable } from 'svelte/store';

function createStrategyButtom() {
    const SW = writable(false);

    return {
        subscribe: SW.subscribe,

        toggle() {
            SW.update($SW => !$SW);
        }
    }
}

export const strategy_buttom = createStrategyButtom();